# 복사해서 .env로 사용 (.env는 커밋하지 말 것)
# 현재 1단계에서는 아무 값도 필요 없음. 이후 단계에서 사용 예정.

# KISA 블랙리스트 (urls.json, hosts.json이 있는 폴더). 비우면 프로젝트 루트 사용
BLACKLIST_DIR=

# RAG (5단계)
ANTHROPIC_API_KEY=
RAG_INDEX_DIR=vector_store
RAG_TOP_K=5
